# snowpark-connect-template-python
A reference Python project template for "Snowpark Connect for Apache Spark".

## Setup

1. Clone the repo
1. `uv venv .venv` (You can change `.venv` to a different name if you prefer)
1. `source .venv/bin/activate`
1. `uv sync`